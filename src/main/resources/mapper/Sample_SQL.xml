<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.littlebank.sample.mybatis.repository.MybatisRepository">

    <insert id="save1" parameterType="com.project.littlebank.sample.mybatis.dto.MybatisDTO">
        insert into aaa( aaa, bbb)
            values (#{aaa}, #{bbb})
    </insert>

    <select id="find1" resultType="String">
        select a.bbb from aaa a
        where a.aaa = #{aaa}
    </select>

</mapper>
